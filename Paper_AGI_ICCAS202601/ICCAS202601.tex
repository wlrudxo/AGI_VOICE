%% Draft manuscript for ICCAS 2026
%% Based on Paper-Template_ICCAS.tex in this directory.

\documentclass[10pt,twocolumn]{ICCAS}

\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{diagbox}
\usepackage{url}

\newcommand{\figureplaceholder}[2]{%
\fbox{%
\begin{minipage}[c][#1][c]{0.94\linewidth}
\centering
\small #2
\end{minipage}}}

\begin{document}

\title{Language-Based Calibration and Bayesian Optimization for\\ MPC Weight Tuning in Vehicle Slalom Simulation}

\author{Kyoungtae Ji${}^{1}$, Dongyeol Won${}^{1}$,  and Kyoungseok Han${}^{1*}$}

\affils{${}^{1}$Department of Automotive Engineering, Hanyang University,\\
Seoul, Korea (kyoungsh@hanyang.ac.kr) {\small${}^{*}$ Corresponding author}}

\abstract{
This paper presents a simulator-in-the-loop study of model predictive control (MPC) weight calibration for an autonomous vehicle slalom maneuver. The controller structure is fixed, and the optimization target is a four-dimensional set of MPC weights governing lateral tracking, heading response, steering effort, and steering smoothness. Each candidate is evaluated by a closed-loop CarMaker-Simulink simulation and converted into a scalar objective including road departure, evaluated cone contacts, lateral tracking error, and heading error. Additional steering and yaw metrics are logged for diagnostic analysis. The study compares non-adaptive sampling, Bayesian optimization (BO), and LLM-based candidate proposal under a common 50-trial budget, and uses Sobol landscape analysis to interpret the resulting search behavior. Results show complementary behavior: LLM-based calibration rapidly reaches physically plausible hit-free regions and performs effective local refinement, whereas BO and Sobol sampling reveal non-intuitive feasible regions that language-only search tends not to explore.
}

\keywords{
Bayesian optimization, large language model, model predictive control, autonomous vehicle simulation, slalom maneuver
}

\maketitle

%-----------------------------------------------------------------------

\section{Introduction}
\label{sec:introduction}

Model predictive control (MPC) is widely used for vehicle control because it can handle multi-objective tracking and actuation constraints in a systematic way. In vehicle applications, control-oriented models are often kept simpler than the high-fidelity plant so that they can be used repeatedly inside an online optimizer or estimator \cite{rajamani2006vehicle,reina2017vehicle,maull2022optimizing}. However, even when the controller structure is fixed, its practical performance depends strongly on the selection of weighting parameters. In high-fidelity simulation, these parameters are often tuned by repeated trial-and-error, which is time-consuming and difficult to reproduce.

This paper considers MPC weight tuning as a black-box optimization problem. A candidate weight vector is applied to the controller, one closed-loop slalom simulation is executed, and the resulting driving performance is summarized by a scalar objective. This formulation is suitable for Bayesian optimization (BO), especially when each evaluation is expensive and derivative information is unavailable. Similar simulation-based optimization ideas have been used in automotive calibration and racing-line computation, where candidate designs are evaluated by a simulator or numerical vehicle model rather than by analytic gradients \cite{jain2020computing,arya2019fully,eriksson2019scalable}.

Recent large language models (LLMs) have shown strong ability to summarize logs, reason over numerical trends, and suggest experimental directions. Recent studies have also investigated LLMs as assistants for BO, scientific experimentation, and autonomous-driving reasoning \cite{liu2024large,cisse2025language,cisse2026can,cui2026llm4ad}. In this work, the LLM is not treated as a direct steering controller. Instead, it is used as an optimization assistant that can interpret previous trials, diagnose failure patterns, and propose candidate regions or warm-start points. This preserves the MPC controller as the low-level control law while investigating whether LLM reasoning can improve the sample efficiency of simulator-based tuning.

The central question is not whether language reasoning replaces numerical optimization. Instead, we ask how language-based calibration behaves differently from BO in a physical closed-loop tuning task. The slalom benchmark is useful for this comparison because feasible hit-free regions can be narrow and sometimes counterintuitive in the controller-weight space. This creates a setting where physically plausible LLM proposals may reach good solutions quickly, while algorithmic exploration may still be needed to reveal separated or non-intuitive local regions.

The contributions of this paper are as follows:
\begin{itemize}
    \item A vehicle slalom MPC weight-tuning task is formulated as a simulator-in-the-loop black-box optimization problem, and a Sobol search-space audit is used to analyze the sparsity and separation of hit-free regions in the four-dimensional log-scaled weight space.
    \item A unified repeated-experiment protocol is presented for comparing LHC, random search, BO, and LLM-based search under the same search range, objective function, and simulation budget.
    \item LLM-based candidate proposal is experimentally evaluated in the tuning loop, where the LLM selects the next MPC weights from vehicle context, driving-task information, and previous trial logs.
\end{itemize}

\section{Problem Formulation and Objective}
\label{sec:problem}

\subsection{Vehicle Dynamics Model}
\label{subsec:vehicle_model}

The MPC controller uses a linearized lateral bicycle model around a constant
longitudinal speed. This class of low-order lateral model is commonly used when
vehicle yaw and lateral motion must be represented with a small number of
states for estimation or control \cite{rajamani2006vehicle,reina2017vehicle}. Let
the state vector be
\begin{equation}
x =
\begin{bmatrix}
v_y & r & e_y & e_\psi
\end{bmatrix}^{\top},
\label{eq:state_vector}
\end{equation}
where $v_y$ is the lateral velocity, $r$ is the yaw rate, $e_y$ is the lateral
tracking error with respect to the reference path, and $e_\psi$ is the heading
error. The manipulated input is the steering-wheel angle command
\begin{equation}
u = \delta_{\mathrm{sw}}.
\label{eq:control_input}
\end{equation}
The simulator plant still uses the high-fidelity CarMaker vehicle model. The
bicycle model is used only as the MPC prediction model, and the optimized
parameters are limited to the MPC weights rather than vehicle parameters or
input scaling factors.

The continuous-time model is written as
\begin{equation}
\dot{x}(t) = A_c x(t) + B_c u(t),
\label{eq:continuous_model}
\end{equation}
with
\begin{equation}
A_c =
\begin{bmatrix}
a_1 & a_2 & 0 & 0 \\
a_3 & a_4 & 0 & 0 \\
1   & 0   & 0 & V_x \\
0   & 1   & 0 & 0
\end{bmatrix},
\quad
B_c =
\begin{bmatrix}
b_1 \\ b_2 \\ 0 \\ 0
\end{bmatrix}.
\label{eq:continuous_matrices}
\end{equation}
The coefficients are
\begin{align}
a_1 &= -\frac{2C_f+2C_r}{mV_x}, \nonumber\\
a_2 &= -\frac{2C_f l_f-2C_r l_r}{mV_x} - V_x, \nonumber\\
a_3 &= -\frac{2C_f l_f-2C_r l_r}{I_z V_x}, \nonumber\\
a_4 &= -\frac{2C_f l_f^2+2C_r l_r^2}{I_z V_x}, \nonumber\\
b_1 &= \frac{2C_f}{m}, \qquad
b_2 = \frac{2C_f l_f}{I_z},
\label{eq:model_coefficients}
\end{align}
where $m$ is the vehicle mass, $I_z$ is the yaw moment of inertia, $l_f$ and
$l_r$ are the distances from the center of gravity to the front and rear axles,
$C_f$ and $C_r$ are the front and rear cornering stiffnesses, and $V_x$ is the
nominal longitudinal speed. The tracking output vector used by the MPC is
\begin{equation}
y =
\begin{bmatrix}
e_y & e_\psi
\end{bmatrix}^{\top}
= Cx,
\quad
C =
\begin{bmatrix}
0 & 0 & 1 & 0 \\
0 & 0 & 0 & 1
\end{bmatrix}.
\label{eq:output_equation}
\end{equation}
The model is discretized with sampling time $T_s$ as
\begin{equation}
x_{k+1} = A_d x_k + B_d u_k,\qquad y_k = Cx_k.
\label{eq:discrete_model}
\end{equation}
This separation between a simple prediction model and a high-fidelity
simulation plant is intentional. The optimizer evaluates the closed-loop result
in the simulator, while the MPC remains lightweight enough for repeated
experiments. Any mismatch between the reduced prediction model and the simulator
plant is therefore handled indirectly through closed-loop performance
evaluation.

\subsection{MPC Formulation}
\label{subsec:mpc_formulation}

At each sampling instant, the MPC predicts the future output trajectory over a
prediction horizon $N_p$ and computes a control sequence over a control horizon
$N_c$. Similar receding-horizon formulations are used in automotive control
problems where actuator limits, rate limits, and tracking or energy objectives
must be considered simultaneously \cite{maull2022optimizing}. The reference path is converted into tracking-error states before
entering the linear MPC. Therefore, the nominal output reference is
\begin{equation}
y^{\mathrm{ref}}_k =
\begin{bmatrix}
0 & 0
\end{bmatrix}^{\top},
\label{eq:reference_output}
\end{equation}
where the two components correspond to zero lateral and heading tracking error.

The finite-horizon quadratic program is
\begin{align}
\min_{\{u_{k+i|k}\}} \quad
& \sum_{i=1}^{N_p}
\left\| y_{k+i|k} - y^{\mathrm{ref}}_{k+i} \right\|_Q^2
\nonumber\\
&+
\sum_{i=0}^{N_c-1}
\left(
\left\| u_{k+i|k} \right\|_R^2
+
\left\| \Delta u_{k+i|k} \right\|_{R_\Delta}^2
\right)
\label{eq:mpc_qp}
\end{align}
subject to
\begin{align}
x_{k+i+1|k} &= A_d x_{k+i|k} + B_d u_{k+i|k}, \nonumber\\
y_{k+i|k} &= Cx_{k+i|k}, \nonumber\\
u_{\min} &\leq u_{k+i|k} \leq u_{\max}, \nonumber\\
\Delta u_{\min} &\leq \Delta u_{k+i|k} \leq \Delta u_{\max},
\label{eq:mpc_constraints}
\end{align}
where
\begin{equation}
\Delta u_{k+i|k} = u_{k+i|k} - u_{k+i-1|k}.
\label{eq:input_increment}
\end{equation}
Only the first command $u^\star_{k|k}$ is applied to the vehicle simulator, and
the optimization is repeated at the next sampling instant. The input constraints
represent the steering-wheel command range used by the simulator interface,
while the input-rate constraints prevent unrealistically discontinuous steering
commands.

The weighting matrices are diagonal:
\begin{equation}
Q = \mathrm{diag}(q_y,q_\psi), \quad
R = r_\delta, \quad
R_\Delta = r_{\Delta\delta}.
\label{eq:mpc_weights}
\end{equation}
The outer-loop optimizer tunes the lateral tracking, heading, steering effort,
and steering-rate smoothness weights. This choice keeps the optimization problem
small and makes the comparison focus on sample-efficient weight selection rather
than on redesigning the controller. It also matches the practical calibration
setting in which an engineer often keeps the controller structure fixed and
searches for weight combinations that provide acceptable safety, tracking, and
smoothness trade-offs.

\subsection{MPC Weight Tuning Problem}
\label{subsec:mpc_weights}

The lateral controller is an MPC controller whose structure, horizons, plant model, and steering constraints are fixed during the experiment. The optimization target is the MPC weight vector
\begin{equation}
\theta =
\left[
q_y,\,
q_\psi,\,
r_\delta,\,
r_{\Delta\delta}
\right]^\top ,
\label{eq:theta}
\end{equation}
where $q_y$ is the lateral tracking weight, $q_\psi$ is the heading error weight, $r_\delta$ is the steering effort weight, and $r_{\Delta\delta}$ is the steering-rate smoothness weight.

The tuning problem is written as
\begin{equation}
\theta^\star =
\arg\min_{\theta \in \Theta} J(\theta),
\label{eq:optimization_problem}
\end{equation}
where $\Theta$ denotes the feasible search space and $J(\theta)$ is obtained from one closed-loop simulation. The search space is logarithmic because useful MPC weights can vary across orders of magnitude. In the formal comparison, each element of $\theta$ is sampled from $[0.01,100]$ in log scale and decoded to the corresponding physical MPC weight before simulation. Steering magnitude and steering-rate limits are kept fixed so that the comparison focuses on controller weighting rather than changing actuator constraints. This produces a small but nontrivial expensive black-box optimization problem, comparable in spirit to automotive calibration and racing-line search tasks where each candidate must be evaluated by a simulation or solver loop \cite{jain2020computing,arya2019fully}.

\subsection{Simulation-Based Objective}
\label{subsec:objective}

The scalar objective is designed to penalize road departure and evaluated cone
contacts first, and then rank runs by lateral and heading tracking accuracy. For
a candidate weight vector $\theta$, let the simulator return a finite time
series over $t \in [0,T]$.

The objective used in the reported experiments is
\begin{align}
J(\theta)
&=
100 I_{\mathrm{fail}}
+ 10 N_{\mathrm{hit}} \nonumber\\
&\quad
+ 4\,\mathrm{RMSE}(e_y)
+ 0.5\,\|e_y\|_{\infty}
+ 5\,\mathrm{RMSE}(e_\psi).
\label{eq:objective}
\end{align}
Here $I_{\mathrm{fail}}$ is one when the vehicle departs from the road and zero
otherwise. $N_{\mathrm{hit}}$ denotes the number of evaluated cone contacts;
entry contacts before the evaluation gate are ignored. The tracking variables
$e_y$ and $e_\psi$ are the lateral and heading errors computed from the
simulation output.

\section{Calibration Search Framework}
\label{sec:framework}

\begin{figure}[t]
\centering
\includegraphics[width=0.8\linewidth]{figures/overview1.png}
\caption{Overview of the shared simulator-in-the-loop MPC weight calibration
workflow.}
\label{fig:overview}
\end{figure}

\subsection{Simulator-in-the-Loop Evaluation}
\label{subsec:sil}

The evaluation loop consists of four steps. First, an optimizer proposes a candidate weight vector $\theta_k$. Second, the weight vector is applied to the MPC controller and a closed-loop slalom simulation is executed. Third, trajectory, steering, yaw, and cone-contact metrics are extracted from the simulation output. Finally, the result pair $(\theta_k,J_k)$ is appended to the experiment history.
Figure~\ref{fig:overview} summarizes this shared simulator-in-the-loop
workflow.

The history after $n$ evaluations is
\begin{equation}
\mathcal{D}_n =
\{(\theta_i,J_i)\}_{i=1}^{n}.
\label{eq:dataset}
\end{equation}
All optimization methods use the same evaluation function, objective definition, and trial budget. This makes the comparison focus on how the next candidate is selected rather than on differences in the simulator or metric extractor.

\subsection{Bayesian Optimization}
\label{subsec:bo}

BO builds a surrogate model from $\mathcal{D}_n$ and selects the next candidate using an acquisition function:
\begin{equation}
\theta_{n+1} =
\arg\max_{\theta \in \Theta}
a(\theta;\mathcal{D}_n).
\label{eq:acquisition}
\end{equation}
In the reported experiments, BO begins with a 15-trial space-filling initial design and then performs sequential acquisition-based updates. This is appropriate for a four-dimensional tuning problem where each simulation is costly but a moderate number of trials is available.

\subsection{LLM-Based Search}
\label{subsec:llm_search}

The LLM-based method uses the previous trial table, best-so-far metrics, and fixed search bounds to propose the next candidate. It does not use a surrogate model, so its performance indicates whether language-based reasoning alone can provide useful tuning suggestions.

\subsection{Sobol Landscape Analysis}
\label{subsec:sobol}

In addition to optimizer comparisons, Sobol low-discrepancy sampling is used to characterize the four-dimensional search landscape. This analysis is not treated as an adaptive optimizer baseline. Its purpose is to estimate whether hit-free feasible solutions form a single intuitive region or multiple separated local regions in log-scaled weight space. This helps interpret why LLM-based and BO trajectories visit different parts of the search space.

Reinforcement learning is not included as a main comparison group. The present problem is static controller-parameter tuning, where one parameter vector is evaluated per rollout. A reinforcement learning baseline would require a state-action policy-learning formulation and a much larger interaction budget, making it a different problem rather than a direct optimizer comparison.

\section{Experimental Setup}
\label{sec:experimental_setup}

\subsection{Slalom Simulation Scenario}
\label{subsec:scenario}

The benchmark scenario is a standard vehicle slalom maneuver with cones. The vehicle is controlled by a Simulink MPC lateral controller in closed-loop co-simulation with a high-fidelity vehicle simulator. The controller output is a steering-wheel angle command, and the same vehicle, road, reference path, controller structure, and physical steering constraints are used for all methods.

The nominal slalom scenario is used as the main benchmark because it exposes meaningful tuning difficulty: many random and space-filling samples finish with one evaluated cone hit, while feasible hit-free completions occupy a narrower region of the weight space. Low-friction variants are reserved for stress testing or future work.

\subsection{Compared Methods}
\label{subsec:methods}

The comparison includes the following methods:
\begin{center}
\begin{tabular}{p{0.30\linewidth}p{0.58\linewidth}}
\hline
Method & Role \\
\hline
LHC & space-filling non-adaptive baseline \\
Random search & simple non-adaptive baseline \\
BO & surrogate-based sequential optimizer \\
LLM-based & language-based candidate proposal \\
\hline
\end{tabular}
\end{center}

Each optimizer method is evaluated under a 50-trial simulation budget. For BO, the budget is divided into a 15-trial initial design and subsequent BO updates. For repeated experiments, independent repetitions are performed under matched stochastic settings when available. The reported baseline set contains five completed repetitions for LHC, random search, BO, and LLM-based search. Sobol sampling uses a larger non-adaptive sample set to analyze feasible-region structure and is not counted as a 50-trial optimizer baseline.

\subsection{Evaluation Metrics}
\label{subsec:metrics}

The results report both objective-level and signal-level metrics:
\begin{itemize}
    \item best objective value versus iteration,
    \item cone contact count and road-departure count,
    \item RMSE and maximum lateral tracking error,
    \item steering command and steering-rate smoothness,
    \item trajectory and steering response of the best run from each method.
\end{itemize}

\section{Results and Discussion}
\label{sec:results}

This section summarizes the nominal slalom experiments. The main optimizer comparison uses
the 50-trial budget and the four-dimensional search space described in
Section~\ref{sec:experimental_setup}. The Sobol analysis is discussed separately
because it uses a larger non-adaptive sample set for landscape interpretation
rather than as a direct 50-trial optimizer.

\subsection{Baseline Results}
\label{subsec:baseline_results}

Table~\ref{tab:method_summary} summarizes the run-level results. Each method is
evaluated as five independent 50-trial repetitions. A hit-free repetition
means that the best candidate found within that 50-trial budget completed the
simulation with zero evaluated cone hits. This run-level view avoids pooling
the five repetitions into a single 250-trial search and better reflects the
calibration outcome available after one assigned budget.

\begin{figure*}[t]
\centering
\includegraphics[width=\linewidth]{figures/fig2_convergence_1x4.pdf}
\caption{Best-so-far objective trajectories over five independent 50-trial
repetitions for each method. Each panel shows one search strategy, and each
curve denotes one repetition.}
\label{fig:convergence}
\end{figure*}

\begin{table}[t]
\caption{Run-level results over five independent 50-trial repetitions. A hit-free repetition is one whose best candidate has zero evaluated cone hits; first hit-free trials are listed only for such repetitions.}
\label{tab:method_summary}
\centering
\begin{tabular}{lrrr}
\hline
Method & Hit-free & First & Median $J$ \\
\hline
LHC & 1/5 & 50 & 11.3927 \\
Random & 0/5 & -- & 11.4301 \\
BO & 3/5 & 19, 39, 33 & 1.3848 \\
LLM-based & 4/5 & 3, 3, 15, 10 & 1.2383 \\
\hline
\end{tabular}
\end{table}

The results show different strengths. LLM-based calibration found a hit-free
best candidate in four of five repetitions and usually did so early in the
budget. BO found hit-free best candidates in three of five repetitions and did
so later, but it discovered the lowest objective value among all 50-trial runs.
Random search did not find a hit-free best candidate in any repetition, and
LHC found one only at the final trial of one repetition. This suggests that
LLM-based calibration is more reliable for rapidly reaching feasible settings,
whereas BO can locate lower-objective local regions when exploration succeeds.

Figure~\ref{fig:convergence} will summarize the same trend over the trial
sequence. The key visual feature expected in this plot is different from a
simple success-rate comparison: LLM-based calibration tends to find feasible
regions early and then improve locally, whereas BO may spend more trials on
infeasible or one-hit samples before locating a lower-objective point.

Table~\ref{tab:best_trials} lists representative best results without exposing
run identifiers. The best BO result used a high heading weight and very low
steering penalties, whereas the best LLM-based result used high tracking and
heading weights with moderate steering-rate penalty. This difference supports
the interpretation that LLM-based search tends to exploit physically intuitive
regions, while BO can enter more aggressive or less intuitive regions.

\begin{table}[t]
\caption{Representative best-run objective metrics from the reported results. Hits denote evaluated cone contacts.}
\label{tab:best_trials}
\centering
\begin{tabular}{@{}lrrrrr@{}}
\hline
Method & $J$ & Hits & RMS$_y$ & Max$_y$ & RMS$_\psi$ \\
\hline
LHC & 1.378 & 0 & 0.210 & 0.767 & 0.031 \\
Random & 11.170 & 1 & 0.168 & 0.794 & 0.020 \\
BO & 0.740 & 0 & 0.101 & 0.538 & 0.013 \\
LLM-based & 1.176 & 0 & 0.171 & 0.784 & 0.021 \\
\hline
\end{tabular}
\end{table}

The repeated runs also show different reproducibility patterns in the weight
space. The three successful BO repetitions did not converge to the same
parameter set. Their best hit-free solutions appeared in separated regions,
including one aggressive high-heading solution and two low-heading solutions
with different steering-penalty combinations. In contrast, the four successful
LLM-based repetitions remained within a more consistent high-heading family:
the exact weights differed across repetitions, but all successful LLM-based
best candidates used moderate-to-high lateral and heading weights with
relatively small steering penalties. Thus, LLM-based calibration is more
reproducible in terms of the type of controller setting it selects, whereas BO
is more diverse and can reveal qualitatively different local optima.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/fig3_best_trajectories.pdf}
\caption{Best-run trajectories for the compared methods. Red triangles denote
slalom cones.}
\label{fig:best_trajectory}
\end{figure}

\subsection{Landscape Analysis}
\label{subsec:landscape_tolerance}

Sobol sampling was added to interpret the above behavior. In this analysis,
1024 Sobol samples were evaluated, with 775 simulation completions and two
hit-free completions. Both hit-free Sobol samples belong to a
low-heading-weight family, with $q_\psi$ close to the lower end of the search
range. Similar low-heading hit-free solutions were also observed in LHC and
BO runs. This family is not a single numerical outlier, but nearby samples often
produce one or more cone hits, indicating a narrow feasible region.

By contrast, the LLM-based successful solutions occupy a more physically
interpretable high-heading family. Across 101 hit-free LLM-based trials, the
successful weights used moderate-to-high lateral and heading weights and low to
moderate steering penalties. Thus, LLM-based search is effective at exploiting a
plausible controller region, but it is less likely to select the low-heading
branch that BO and Sobol sampling can reveal.

\begin{figure*}[t]
\centering
\includegraphics[width=0.98\textwidth]{figures/fig4_hit_free_regions.pdf}
\caption{Representative projections of hit-free candidates in the log-scaled MPC
weight space. The green shaded ellipse denotes the empirical high-heading region
visited by LLM-based search, while LHC and BO are shown as sparse hit-free
candidates. Black center markers R1--R6 denote representative candidates selected
for interpretation. Sobol audit samples are excluded from the plot and summarized
separately in the text.}
\label{fig:local_regions}
\end{figure*}

\begin{table*}[t]
\caption{Representative hit-free solution families found during the search. The
representatives summarize distinct hit-free regions, not only final best
candidates.}
\label{tab:solution_families}
\centering
\scriptsize
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}llrrrrr>{\raggedright\arraybackslash}p{0.40\textwidth}@{}}
\hline
Rep. & Found by & $J$ & $q_y$ & $q_\psi$ & $r_\delta$ & $r_{\Delta\delta}$ & Interpretation \\
\hline
R1 & BO & 0.7399 & 13.32 & 89.76 & 0.011 & 0.013 & dominant heading correction with very weak steering penalties \\
R2 & LLM-based & 1.1764 & 80.00 & 45.77 & 0.100 & 0.800 & strong lateral/heading tracking with moderate steering smoothing \\
R3 & BO & 1.3779 & 62.96 & 4.71 & 0.190 & 1.130 & lateral-error-dominant tracking with smoothed steering motion \\
R4 & BO & 1.3714 & 56.00 & 0.046 & 0.350 & 0.951 & low-heading solution with stronger steering and rate penalties \\
R5 & LHC & 1.3783 & 29.11 & 0.23 & 0.150 & 0.530 & lateral tracking with little direct heading-error correction \\
R6 & BO & 1.3848 & 86.59 & 0.041 & 0.021 & 1.758 & lateral-error-heavy control with weak steering magnitude penalty but strong rate smoothing \\
\hline
\end{tabular*}
\end{table*}

Table~\ref{tab:solution_families} summarizes representative hit-free candidates
selected from all hit-free trials observed during the search. R2 summarizes the
LLM-based high-heading region, where many successful trials remain near the same
physically plausible controller setting. In contrast, BO produced several
separated hit-free candidates, including high-heading, mid-heading, and
low-heading solutions with different steering-penalty combinations. These
representatives are not additional methods; they summarize distinct weight
patterns observed among the hit-free candidates.

The overall result therefore supports a limited but useful conclusion. LLM-based
calibration is effective as a fast feasibility-seeking and local-refinement
mechanism. BO is less aligned with physical intuition but can find lower-cost
or more diverse local regions. These observations motivate future hybrid
methods, but the present paper focuses on the behavior of LLM-based calibration
and BO rather than claiming a completed hybrid optimizer.

\section{Conclusion}
\label{sec:conclusion}

This paper formulates MPC weight tuning for a vehicle slalom maneuver as a simulator-in-the-loop black-box optimization problem. The proposed framework evaluates each candidate through closed-loop simulation, computes a scalar objective from road departure, evaluated cone contacts, and tracking metrics, and logs yaw and steering metrics for diagnostic analysis under a common candidate-selection budget.

The nominal slalom results indicate that LLM-based search can rapidly find feasible hit-free MPC weights and refine physically plausible controller settings, while BO can discover lower-objective and less intuitive regions but with lower feasibility frequency. The Sobol analysis indicates that the objective landscape contains multiple local minimum regions: hit-free solutions appear in both high-heading regions favored by LLM-based search and low-heading regions that are easier for non-language exploration to reveal. Future work will focus on exploration-preserving hybrid designs, low-friction scenarios, additional maneuvers, and reinforcement learning only if the task is reformulated as a policy-learning problem.

\bibliographystyle{ieeetr}
\bibliography{ref}

\end{document}
