# Prompt Seeds

이 asset은 `carmaker-python-runtime` skill을 호출할 때 바로 써먹을 수 있는 사용자 프롬프트 예시를 모은다.

- Python으로 variation을 실행하고 끝날 때까지 기다리고 싶어.
- SimControlInteractive로 quantity condition을 기다리고 싶어.
- capture를 파일로 저장하는 흐름을 알려줘.